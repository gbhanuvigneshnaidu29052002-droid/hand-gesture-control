# Configuration file for camera and control parameters

# Camera resolution
CAMERA_WIDTH = 640
CAMERA_HEIGHT = 480

# Region reduction to avoid full screen mapping (stability)
FRAME_REDUCTION = 100

# Smoothening factor for cursor movement (higher = smoother but slower)
SMOOTHENING = 7

# Delay between clicks to avoid rapid multiple clicks
CLICK_DELAY = 0.3